# Frontend (React + Vite)

Rápido para rodar:

1. Pré-requisito: Node.js e npm
2. Dentro da pasta `frontend`:

```powershell
npm install
npm run dev
```

- Frontend: http://localhost:5173
- Para usar outra API: crie `frontend/.env` com `VITE_API_URL=http://seu_host:8000`
